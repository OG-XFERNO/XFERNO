{
  "name": "@xferno/circuits",
  "version": "0.1.0",
  "private": true,
  "description": "XFERNO ZK Rollup Circuits",
  "scripts": {
    "compile": "circom circuits/main.circom --r1cs --wasm --sym -o build",
    "setup": "snarkjs groth16 setup build/main.r1cs pot12_final.ptau build/main_0000.zkey",
    "contribute": "snarkjs zkey contribute build/main_0000.zkey build/main_final.zkey --name=\"XFERNO Phase 1\" -v",
    "export-verifier": "snarkjs zkey export solidityverifier build/main_final.zkey ../src/zk/Verifier.sol",
    "export-vkey": "snarkjs zkey export verificationkey build/main_final.zkey build/verification_key.json",
    "prove": "node scripts/prove.js",
    "verify": "node scripts/verify.js"
  },
  "devDependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  }
}
